# Online platform game base

You can set these variables:

```
> src/core/store/globals.ts
(game ref variable)

> src/core/scripts/canvas.ts
(canvas, firstPlayerMoveBgPosition, canReturnBack, createBackground(img source))

```
